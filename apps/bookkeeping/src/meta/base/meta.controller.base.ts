/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { MetaService } from "../meta.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MetaCreateInput } from "./MetaCreateInput";
import { MetaWhereInput } from "./MetaWhereInput";
import { MetaWhereUniqueInput } from "./MetaWhereUniqueInput";
import { MetaFindManyArgs } from "./MetaFindManyArgs";
import { MetaUpdateInput } from "./MetaUpdateInput";
import { Meta } from "./Meta";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class MetaControllerBase {
  constructor(
    protected readonly service: MetaService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Meta })
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: MetaCreateInput,
  })
  async create(@common.Body() data: MetaCreateInput): Promise<Meta> {
    return await this.service.create({
      data: data,
      select: {
        active: true,
        bankAccount: true,
        category: true,
        createdAt: true,
        email: true,
        id: true,
        name: true,
        paymentTerms: true,
        paymentType: true,
        taxId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Meta] })
  @ApiNestedQuery(MetaFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Meta[]> {
    const args = plainToClass(MetaFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        active: true,
        bankAccount: true,
        category: true,
        createdAt: true,
        email: true,
        id: true,
        name: true,
        paymentTerms: true,
        paymentType: true,
        taxId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Meta })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: MetaWhereUniqueInput
  ): Promise<Meta | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        active: true,
        bankAccount: true,
        category: true,
        createdAt: true,
        email: true,
        id: true,
        name: true,
        paymentTerms: true,
        paymentType: true,
        taxId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Meta })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: MetaUpdateInput,
  })
  async update(
    @common.Param() params: MetaWhereUniqueInput,
    @common.Body() data: MetaUpdateInput
  ): Promise<Meta | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          active: true,
          bankAccount: true,
          category: true,
          createdAt: true,
          email: true,
          id: true,
          name: true,
          paymentTerms: true,
          paymentType: true,
          taxId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Meta })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: MetaWhereUniqueInput
  ): Promise<Meta | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          active: true,
          bankAccount: true,
          category: true,
          createdAt: true,
          email: true,
          id: true,
          name: true,
          paymentTerms: true,
          paymentType: true,
          taxId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
