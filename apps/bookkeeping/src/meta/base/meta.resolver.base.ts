/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { CreateMetaArgs } from "./CreateMetaArgs";
import { UpdateMetaArgs } from "./UpdateMetaArgs";
import { DeleteMetaArgs } from "./DeleteMetaArgs";
import { MetaCountArgs } from "./MetaCountArgs";
import { MetaFindManyArgs } from "./MetaFindManyArgs";
import { MetaFindUniqueArgs } from "./MetaFindUniqueArgs";
import { Meta } from "./Meta";
import { MetaService } from "../meta.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Meta)
export class MetaResolverBase {
  constructor(
    protected readonly service: MetaService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "read",
    possession: "any",
  })
  async _metasMeta(
    @graphql.Args() args: MetaCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Meta])
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "read",
    possession: "any",
  })
  async metas(@graphql.Args() args: MetaFindManyArgs): Promise<Meta[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Meta, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "read",
    possession: "own",
  })
  async meta(@graphql.Args() args: MetaFindUniqueArgs): Promise<Meta | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Meta)
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "create",
    possession: "any",
  })
  async createMeta(@graphql.Args() args: CreateMetaArgs): Promise<Meta> {
    return await this.service.create({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Meta)
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "update",
    possession: "any",
  })
  async updateMeta(@graphql.Args() args: UpdateMetaArgs): Promise<Meta | null> {
    try {
      return await this.service.update({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Meta)
  @nestAccessControl.UseRoles({
    resource: "Meta",
    action: "delete",
    possession: "any",
  })
  async deleteMeta(@graphql.Args() args: DeleteMetaArgs): Promise<Meta | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
