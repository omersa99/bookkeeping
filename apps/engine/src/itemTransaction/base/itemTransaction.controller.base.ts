/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ItemTransactionService } from "../itemTransaction.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ItemTransactionCreateInput } from "./ItemTransactionCreateInput";
import { ItemTransactionWhereInput } from "./ItemTransactionWhereInput";
import { ItemTransactionWhereUniqueInput } from "./ItemTransactionWhereUniqueInput";
import { ItemTransactionFindManyArgs } from "./ItemTransactionFindManyArgs";
import { ItemTransactionUpdateInput } from "./ItemTransactionUpdateInput";
import { ItemTransaction } from "./ItemTransaction";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ItemTransactionControllerBase {
  constructor(
    protected readonly service: ItemTransactionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ItemTransaction })
  @nestAccessControl.UseRoles({
    resource: "ItemTransaction",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: ItemTransactionCreateInput
  ): Promise<ItemTransaction> {
    return await this.service.create({
      data: {
        ...data,

        entity: data.entity
          ? {
              connect: data.entity,
            }
          : undefined,

        invoiceModel: data.invoiceModel
          ? {
              connect: data.invoiceModel,
            }
          : undefined,

        item: data.item
          ? {
              connect: data.item,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        entity: {
          select: {
            id: true,
          },
        },

        id: true,

        invoiceModel: {
          select: {
            id: true,
          },
        },

        item: {
          select: {
            id: true,
          },
        },

        quantity: true,
        totalAmount: true,
        unitCost: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ItemTransaction] })
  @ApiNestedQuery(ItemTransactionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ItemTransaction",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<ItemTransaction[]> {
    const args = plainToClass(ItemTransactionFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,

        entity: {
          select: {
            id: true,
          },
        },

        id: true,

        invoiceModel: {
          select: {
            id: true,
          },
        },

        item: {
          select: {
            id: true,
          },
        },

        quantity: true,
        totalAmount: true,
        unitCost: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ItemTransaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemTransaction",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: ItemTransactionWhereUniqueInput
  ): Promise<ItemTransaction | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,

        entity: {
          select: {
            id: true,
          },
        },

        id: true,

        invoiceModel: {
          select: {
            id: true,
          },
        },

        item: {
          select: {
            id: true,
          },
        },

        quantity: true,
        totalAmount: true,
        unitCost: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ItemTransaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemTransaction",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: ItemTransactionWhereUniqueInput,
    @common.Body() data: ItemTransactionUpdateInput
  ): Promise<ItemTransaction | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          entity: data.entity
            ? {
                connect: data.entity,
              }
            : undefined,

          invoiceModel: data.invoiceModel
            ? {
                connect: data.invoiceModel,
              }
            : undefined,

          item: data.item
            ? {
                connect: data.item,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          entity: {
            select: {
              id: true,
            },
          },

          id: true,

          invoiceModel: {
            select: {
              id: true,
            },
          },

          item: {
            select: {
              id: true,
            },
          },

          quantity: true,
          totalAmount: true,
          unitCost: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ItemTransaction })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ItemTransaction",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: ItemTransactionWhereUniqueInput
  ): Promise<ItemTransaction | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,

          entity: {
            select: {
              id: true,
            },
          },

          id: true,

          invoiceModel: {
            select: {
              id: true,
            },
          },

          item: {
            select: {
              id: true,
            },
          },

          quantity: true,
          totalAmount: true,
          unitCost: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
